services:
  grinder-web:
    build: .
    container_name: grinder-web
    restart: unless-stopped
    environment:
      GRINDER_DATA_DIR: /app/data
    volumes:
      - grinder_data:/app/data
    ports:
      - "18088:8080"

volumes:
  grinder_data:
